name: Build Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Production Image
    uses: zavestudios/platform-pipelines/.github/workflows/image-build.yml@c0aa80bb405fa24ede5ebcf1c40b0be90328386c
    with:
      context: .
      dockerfile: production.Dockerfile
      tags: thehouseguy:ci
