name: Tests

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Run Test Suite
    uses: zavestudios/platform-pipelines/.github/workflows/rails-test.yml@c0aa80bb405fa24ede5ebcf1c40b0be90328386c
    with:
      ruby_version: '3.3.2'
      run_system_tests: false
