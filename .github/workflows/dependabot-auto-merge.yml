name: Dependabot

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    uses: zavestudios/platform-pipelines/.github/workflows/dependabot-auto-merge.yml@c0aa80bb405fa24ede5ebcf1c40b0be90328386c
    with:
      auto_merge_major: false
      auto_merge_minor: true
      auto_merge_patch: true
      merge_method: 'squash'
